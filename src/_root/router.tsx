import React from "react";
import { Route, BrowserRouter } from "react-router-dom";
import App from "./App";

const router = (
  <BrowserRouter>
    <App />
  </BrowserRouter>
);

export default router;
